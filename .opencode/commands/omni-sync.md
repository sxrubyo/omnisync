---
description: Portable migration control plane -- show the command center or route to an Omni flow
---

Omni Sync router. Arguments provided: "$ARGUMENTS"

If arguments are empty, show the Omni command center.

Otherwise, load the Omni skill and route through the CLI:

```text
skill({ name: "omni-sync" })
```

Prefer these commands:
- `omni guide`
- `omni connect`
- `omni briefcase --full`
- `omni restore`
- `omni push`
- `omni pull`
